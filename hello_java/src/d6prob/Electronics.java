package d6prob;

public interface Electronics {
    boolean Switchable(boolean isSwitch);
    boolean PlaySount(boolean isPlaying);
    boolean Display(boolean isOn);
    boolean CookingUtensil(boolean isCooking);
}
